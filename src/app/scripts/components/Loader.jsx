import React, { Component, PropTypes } from 'react';

class Loader extends Component {
    constructor(props) {
        super(props);
    }
    
    render() {
        return (
            <div>Loading</div>
        );
    }
}

export default Loader;