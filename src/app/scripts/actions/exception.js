import { ERROR } from '../constants';
import EXCEPTIONS from '../exceptions';

export function exception(err) {
    // if (EXCEPTIONS[err]) {
    //     return {
    //         type: '',
    //         payload: ''
    //     }
    // }
}